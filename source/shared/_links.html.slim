.links
  .wrapper
    h2 = t 'links.title'
    .more
      - t('links.more').each do |id, links|
        = link_to links[:url], class: "#{id}", target: "_blank"
          h3 = links[:title]
          p = links[:title]
    ul
      - t('links.school').each do |id, links|
        li
          = link_to links[:url], class: "#{id}", target: "_blank"
            = image_tag "images/school/#{id}.png"
